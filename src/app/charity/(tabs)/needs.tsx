import { Text } from 'react-native-paper';

import ThemedView from '@/src/components/ThemedView';

export default function Needs() {
  return (
    <ThemedView>
      <Text>Edit app/charity/(tabs)/needs.tsx to modify this page</Text>
    </ThemedView>
  );
}
